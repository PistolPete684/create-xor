function xor(a, b) {
  // use the 'does not equal' comparison to return true when different and false when the same
  // therefore; !== is equalivent to the xor operator 
  return a !== b;
}

// const xor=(a, b)=>a!=b;